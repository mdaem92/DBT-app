import UserActionTypes from './user.types'
export const UserReducer = (state={},action)=>{
    switch (action.type) {
        case value:
            
            break;
    
        default:
            break;
    }
}